const fs = require('fs');
const path = require('path');

// NOTE: Footer is now managed by js/components.js (SiteFooter custom element).
// Edit the footer HTML in js/components.js instead of running this script.

console.log('Footer is now in js/components.js — edit it there directly.');
